# Spargo runs entirely client-side — no env vars are required to run it.
# Vercel Analytics is auto-configured on Vercel deployments (no key needed).
# Add project-specific secrets below as the app grows.
